= format_message_fragment(@opts[:opening_message] || 'You are scheduled to perform at an upcoming New Ruckus Composer Night:')

- missing_info_urgent = @signup.composer_night.start_time < 4.days.from_now

= render 'time_and_location'
= render 'info_provided' unless missing_info_urgent
= render 'required_info_missing'
= render 'optional_info_missing'
= render 'info_provided' if missing_info_urgent

%p
  To add to or change any of the above:

= render 'edit_info_link'

= format_message_fragment(@opts[:closing_message] || 'Looking forward to hearing your music!')
